8	0	предисловие
12	1	название игры
14	2	издание книг и печатание текстов
18	3	управление системой TeX
26	4	выбор шрифта
34	5	группарование
40-1	6	работаем с системой TeX
56-2	7	как TeX читает то, что вы напечатали
64-2	8	печатаем символы
72-2	9	латинские шрифты системы TeX
78-2	10	задаем размеры
86-3	11	рамки
92-3	12	клей
110-3	13	режим работы
118	14	как TeX разбивает абзацы на троки
138	15	как TeX разбивает строки в страницы
158	16	печатаем математические формулы
170	17	и еще о математике
194	18	математика: тонкости набора
220	19	выделенные формулы
236-5	20	определения или макрокоманды
260	21	строим рамки
272	22	выравнивание
294	23	программа вывода
310-6	24	вертикальный режим работы
332	25	горизонтальный режим работы
338	26	математический режим работы
346	27	исправляем ошибки
358-6	A	ответы к упражнениям
394	B	основные управляющие последовательности
424	C	коды символов
430	D	необычные трюки
462	E	примеры форматов
488-7	F	таблицы шрифтов
502-7	G	генерируем рамки из формул
512-7	H	перенос слов
522-7	I	предметный указатель
548	J	сообщество


\n<=> ; %comment\n<=>;
** - \input можно не писать
\input MS - вставить MS.tex, если файл начинается с &, то это файл формата
\eject - отправить страницу в выходной файл
\relax - ничего не делать 
\end - конец
\show\команда - выводит в консоль код команды
\showthe\регистр - (?) подставляет значение регистра
\nullfont - шрифт, который не содержит символов
\catcode - задание категрии кода (символа)

- - дефис - в словах
-- - N-тире - в диапозонах
--- - M-тире - в предложениях
$-$ - минус
---- - (?упр 2.2) --- -

TeX автоматически заменяет лигатуры и кернинг (fi AV и т.п.), также как --

`x' - кавычки
``x'' - двойные кавычки
` = \lq
' = \rq
\'x - x под ударением справа
\`x - x под ударением слева
\"o - о как ё (умляут)
\c c - с с завитушкой снизу (седиль)
> \c=macro:#1->{
	\setbox \z@ \hbox {#1}
	\ifdim \ht \z@ =1ex\accent 24 #1
	\else \ooalign {\unhbox \z@ \crcr \hidewidth \char 24\hidewidth }
	\fi
}. ппц...


\thinspace - тонкий пробел (macro ->\kern .16667em)
~ - неразрывный пробел
\  		- пробел (> \^^M=macro:->\ .)
\<return> 	- ???    (> \^^M=macro:->\ .)

\команда пробел аргумент, команда состоит только из букв (без цифр)
\знак аргумент
несколько пробелов = 1 пробел
\пробел - пробел
\TeX\ игнорирует пробелы после управляющих слов
{\TeX} = \TeX{}
{} - пустая группа, ничего не делает, предотвращает от съедания пробела и для разделения лигатур
в приложении F таблицы математических символов
около 900 встроенных управляющих последовательностей, из них примерно 300 примитивных
в приложении B 600 встроенных управляющих последовательностей
в приложении E 
в главе 20 объясняется, как делать собственные управляющие последовательности

изменение шрифта:
\rm - (roman) обычный
\sl - (slanted) наклонный
\it - (italic) курсивный
\tt - (typewriter) моноширинный
\bf - (bold) жирный (> \bf=macro:->\fam \bffam \tenbf . (> \bffam=\char"6.)(> \tenbf=select font cmbx10.))
{\bf ...} = \bf ... \rm
\/ - надо использовать перед пробелом для перехода от курсива или наклонного к обычному или жирному, если следующим символом не чявляется точка или запятая. например `{\bf f\/}'
\tenrm \ninerm \tensl \ninesl ... - выбор шрифтов в 10 и 9 пунктов
\font\команда=название_шрифта - задание команды выбор шрифта
\font\команда=название_шрифта at <размер> - задание команды выбор шрифта + масштабирование к заданному размеру
\font\команда=название_шрифта scaled <число> - задание команды выбор шрифта + масштабирование в заданное число раз/1000
<размер> - например 10pt
\magstep0 = 1000
\magstephalf = 1095
\magstep1 = 1200
\magstep2 = 1440
\magstep3 = 1728
\magstep4 = 2074
\magstep5 = 2488
в главе 10 - как увеличивать весь документ

\centerline блок (> \centerline=macro:#1->\line {\hss #1\hss }.(> \line=macro:->\hbox to\hsize .))
	за блок ситается {} или одна буква
\count0 - регистр страниц
\global команда - команда выполняется глобально (во всех текущих группах) а не в текущем блоке
\advance\count0 by 1
управляющие последовательности могут открывать и закрывать группы
при помощи \begingoup и \endgroup.
\def\beginthe#1{\begingroup\def\blockname{#1}}
\def\endthe#1{\def\test{#1}
	\ifx\test\blockname\endgroup
	\else\errmessage{здесь следует написать \string\endthe{\blockname}}
	\fi
}


пустая строка = \par
\medbreak - промежуток между строк (> \medbreak=macro:->
	\par \ifdim \lastskip <\medskipamount \removelastskip \penalty -100\medskip \fi .)
	(> \medskipamount=\skip14.)
	(> \removelastskip=macro:->\ifdim \lastskip =\z@ \else \vskip -\lastskip \fi .)
	(> \medskip=macro:->\vskip \medskipamount .)
\vskip размер - пропустить размер по вертикали (? где в грамматике ?)
\vfill - заполнить страницу пустым пространством
\hsize=размер - ширина текста
	Overfull \hbox - слижком узкие или слижком широкие пробелы
\hrule - горизонтальная черта
ga\-axy <=> \hiphenation{ga-laxy} - перенос, только во втором случае он запоминает навсегда как переносить galaxy
\raggedright - не выравнивать по правому краю 
	(> \raggedright=macro:->\rightskip \z@ plus2em \spaceskip .3333em \xspaceskip .5em\relax .)
\tolerance=ц-число - максимальный размер пробелов, влияет на переполнение строки
	при переполнении строки он вконце дописывает жирный квадрат
	10000=infty
\hfuzz=размер - допустимый выход строки вправо (по умолчанию 0.1pt)
\badness=ц-число - если негодность пробелов в строке превышает это число, то выдает предупреждение
	-1 - предупреждать о каждой строке
	в plain-tex по умолчанию 200

\def команда блок - макрос
\def команда {{блок}} - {макрос}
\def команда#1#2{блок, где #1, #2 - параметры}
9 параметров, указывать надо по порядку, ## -> #
\def команда #1паттерн1#2паттерн2{блок, где #1, #2 - параметры}
вызов: \команда арг паттерн1 арг паттерн2 (блоки - как 1 символ)






